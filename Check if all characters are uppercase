#include <iostream>
#include <string>
using namespace std;

int main() {
    string s;
    cin >> s;

    bool ok = true;

    for(char c : s) {
        if(c < 'A' || c > 'Z') {
            ok = false;
            break;
        }
    }

    cout << (ok ? "YES" : "NO");

    return 0;
}
