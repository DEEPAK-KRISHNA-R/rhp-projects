#include <iostream>
#include <string>
using namespace std;

int main() {
    string s;
    cin >> s;

    bool ok = true;

    for(char c : s) {
        if(c < '0' || c > '9') {
            ok = false;
            break;
        }
    }

    cout << (ok ? "YES" : "NO");

    return 0;
}
